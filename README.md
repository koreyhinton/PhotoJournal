# PhotoJournal

Android app in the works. Kotlin code is bash-assembled using [orc](https://github.com/koreyhinton/orc).

## Prebuild

First, prebuild with `build.sh`, then open in Android studio.

```sh
./build.sh
```





